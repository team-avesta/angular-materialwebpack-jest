'use strict';

export default function(app) {

    app.factory('forgotPasswordService', service);

    function service() {
        var service = {};

        return service;

        ////////////
        ///start writing function implementations below
    }
}